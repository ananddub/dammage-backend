# Hackathon broker — anonymous public access.
# Rotate to a user/pass + TLS config before production.

listener 1883 0.0.0.0
allow_anonymous true

# WebSockets listener — browser MQTT clients can subscribe directly.
listener 9001 0.0.0.0
protocol websockets

persistence true
persistence_location /mosquitto/data/
log_dest stdout
